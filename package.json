{
  "name": "co-promisify",
  "description": "wrap a generator to make it a promise returning function",
  "version": "0.0.1",
  "author": "Arpad Borsos <arpad.borsos@googlemail.com> (http://swatinem.de)",
  "license": "GPLv3",
  "repository": "Swatinem/co-promisify",
  "dependencies": {
  },
  "devDependencies": {
    "nlz": "0",
    "mocha": "1",
    "chai": "1"
  },
  "scripts": {
    "build": "node --harmony node_modules/.bin/nlz build --standalone co",
    "test": "npm run-script build && mocha --harmony --reporter spec --bail",
  }
}
